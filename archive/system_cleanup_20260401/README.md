# System Cleanup Archive 2026-04-01

This archive stores system-generated and clutter-like files moved out of the working tree during cleanup.

Included categories:

- LaTeX build artifacts (`*.aux`, `*.bbl`, `*.blg`, `*.fdb_latexmk`, `*.fls`, `*.out`, `*.toc`, `*.xdv`)
- Run and shell logs (`*.log`)
- macOS metadata files (`.DS_Store`)

Total archived files: 52

Scope note:

- Raw experiment outputs, curated study artifacts, analysis CSVs, figures, PDFs, and source `.tex` / `.bib` / `.md` files were intentionally left in place.
- This cleanup only moved clutter-like or system-generated byproducts so that existing research content and references remain stable.

See `manifest.csv` for the full source-to-archive mapping.
